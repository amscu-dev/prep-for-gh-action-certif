name: First Workflow
# workflow_dispatch - eveniment declansat de utilizator;
# TRIGGER:
on: workflow_dispatch
# JOBS:
jobs:
  first-job: 
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!"
      - name: Print goodbye
        run: echo "Done - bye!"
